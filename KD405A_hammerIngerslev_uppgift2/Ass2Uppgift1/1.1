Skapa en textfil där du svarar på följande frågor (Lägg filen i det projekt du skall skapa i uppgift 1.2).
Ange vad som är:

a) konstruktor

public House(int yearBuilt,int size){
}

b) instansvariabler

private int yearBuilt;
 private int size;
 private final int min size;
 
c) instansmetoder

 public int getYearBuilt(){
            }
 
 public int getSize(){
             }
 
d) vad innebär det att minSize är deklarerad final?

At bruge en final int betyder/medvirker at den ikke kan ændres af programmet.


e) vad innebär det att vissa koddelar är deklarerade private och andra public?
EN private variable, kan kun kun nåes indenfor klassen, en public kan hentes "udefra".

f) Rita ett UML diagram över klassen (visa denna för handledaren).
